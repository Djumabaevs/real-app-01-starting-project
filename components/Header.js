import React from "react";
import {} from 'react-native';

const Header = props => {
    return(
        <View>
            <Text>{}</Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default Header;